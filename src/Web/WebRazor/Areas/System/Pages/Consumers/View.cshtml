@page "{id}"
@model WebRazor.Areas.Systems.Pages.Consumers.ViewModel
@{
}

<consumers-view uid="@Model.UserId"
                    id="@Model.Id"
                    role-id="system"></consumers-view>